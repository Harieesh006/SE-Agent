version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: se-agent-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: se_agent

volumes:
  mongo_data:
